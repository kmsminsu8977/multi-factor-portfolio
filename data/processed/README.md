# Processed Data

정제 데이터 보관 위치입니다.

현재 baseline은 정제 중간 파일을 별도로 저장하지 않고 `data/sample/factor_input_panel.csv`에서 바로 `outputs/tables` 산출물을 만듭니다. 실제 원천 엑셀을 연결할 경우 가격/재무제표를 결합한 중간 패널을 이 폴더에 저장하고, 어떤 시점 기준으로 모멘텀과 재무 특성을 계산했는지 기록합니다.
